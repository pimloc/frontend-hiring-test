const boxA = { id: 1, left: 0.1, top: 0.1, height: 0.1, width: 0.1 };
const boxB = { id: 2, left: 0.2, top: 0.2, height: 0.1, width: 0.2 };
const boxC = { id: 3, left: 0.3, top: 0.3, height: 0.2, width: 0.1 };

const defaultState = {
  boxes: [boxA, boxB, boxC]
};

export default defaultState;
